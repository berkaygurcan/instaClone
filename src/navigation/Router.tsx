import React from 'react';
import {Login} from '../screens';
import {View} from 'react-native';

const Router = () => {
  return (
    <View>
      <Login />
    </View>
  );
};

export default Router;
